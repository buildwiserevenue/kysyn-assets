name: Resilient Build 100+ Grammars
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  build-all-wasm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node & Emscripten
        uses: actions/setup-node@v4
        with: { node-version: '20' }
      
      - name: Install Tree-sitter CLI
        run: npm install -g tree-sitter-cli

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with: { version: 3.1.64 }

      # --- 1. CACHE  ---
      - name: Cache Tree-sitter WASI SDK
        uses: actions/cache@v4
        with:
          path: /home/runner/.cache/tree-sitter
          key: tree-sitter-wasi-v29

      # --- 2. BUILD LOOP ---
      - name: Build Loop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p grammars
          [ -f versions.json ] || echo "{}" > versions.json
          sed -i 's/\r//' languages.txt

          grep -v '^#' languages.txt | while read -r repo_entry; do
            [ -z "$repo_entry" ] && continue
            
            if [[ "$repo_entry" == *":"* ]]; then
              base_repo="${repo_entry%%:*}"
              subdir="${repo_entry##*:}"
              lang_name=$(basename "$subdir")
            else
              base_repo="$repo_entry"
              subdir=""
              lang_name=$(basename "$base_repo" | sed 's/tree-sitter-//')
            fi

            echo "--------------------------------------------"
            echo "🔍 Checking $lang_name ($base_repo)..."

            REMOTE_SHA=$(gh api repos/$base_repo/commits/HEAD --template '{{.sha}}' 2>/dev/null || echo "ERROR")
            
            if [ "$REMOTE_SHA" == "ERROR" ]; then
              echo "⚠️ Repo non trovato o errore API per $base_repo. Salto."
              continue
            fi

            LOCAL_SHA=$(jq -r ".\"$lang_name\"" versions.json)

            if [ "$REMOTE_SHA" == "$LOCAL_SHA" ] && [ -f "grammars/$lang_name.wasm" ]; then
              echo "⏭️ $lang_name già aggiornato."
              continue
            fi

            echo "🏗️ Building $lang_name..."
            rm -rf temp_repo
            if ! git clone --depth 1 --retry 3 "https://github.com/$base_repo.git" temp_repo --depth 1 2>/dev/null; then
              echo "❌ Clone failed per $base_repo"
              continue
            fi
            
            cd temp_repo
            [ -n "$subdir" ] && cd "$subdir"
            
            tree-sitter generate 2>/dev/null || echo "Info: generate non necessario."

            if tree-sitter build --wasm; then
              generated_wasm=$(find . -maxdepth 1 -name "*.wasm" | head -n 1)
              if [ -n "$generated_wasm" ]; then
                mv "$generated_wasm" "$GITHUB_WORKSPACE/grammars/$lang_name.wasm"
                cd "$GITHUB_WORKSPACE"
                jq ".\"$lang_name\" = \"$REMOTE_SHA\"" versions.json > tmp.json && mv tmp.json versions.json
                echo "✅ Success: $lang_name"
              fi
            else
              echo "❌ Build failed per $lang_name"
            fi
            
            cd "$GITHUB_WORKSPACE"
            rm -rf temp_repo
            sleep 6
          done

      # --- 4. COMMIT RISULTATI ---
      - name: Commit Results
        if: always() 
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 Smart Update: Progressi build WASM"
          file_pattern: 'grammars/*.wasm versions.json'
